export const config = {
  github: {
    login: 'glink25', // github login name, not user name
    repo: "test-for-anything", //"urodele",
  },
  head: {
    title: 'Urodele'
  }
} as const;

export default config;
