export const config = {
  github: {
    login: 'glink25', // github login name, not user name
    repo: "glink25.github.io", //"urodele",
  },
  head: {
    title: 'Urodele'
  }
} as const;

export default config;
